# ========================================# ========================================

# WhatsApp Gateway Environment Variables# WhatsApp Gateway Environment Variables

# untuk Production di Server VPS (194.59.165.252)# untuk Production di Server VPS (194.59.165.252)

# ========================================# ========================================



# Node.js Configuration# Node.js Configuration

NODE_ENV=productionNODE_ENV=production

PORT=8086PORT=8086



# WhatsApp Configuration# WhatsApp Configuration

# Format: 62xxxxxxxxxx (tanpa +)# Format: 62xxxxxxxxxx (tanpa +)

WA_SENDER=62xxxxxxxxxxWA_SENDER=62xxxxxxxxxx



# Telegram Bot (Optional - untuk notifikasi QR Code)# Telegram Bot (Optional - untuk notifikasi QR Code)

# Dapatkan dari @BotFather di Telegram# Dapatkan dari @BotFather di Telegram

TELEGRAM_BOT_TOKEN=your_telegram_bot_token_hereTELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

TELEGRAM_CHAT_ID=your_telegram_chat_id_hereTELEGRAM_CHAT_ID=your_telegram_chat_id_here



# Laravel Integration# Laravel Integration

LARAVEL_API_URL=http://ekomh29.biz.id/apiLARAVEL_API_URL=http://ekomh29.biz.id/api

LARAVEL_WEBHOOK_URL=http://ekomh29.biz.id/api/webhook/whatsappLARAVEL_WEBHOOK_URL=http://ekomh29.biz.id/api/webhook/whatsapp

# Optional - untuk production dengan authentication# Optional - untuk production dengan authentication

# Generate: openssl rand -hex 32# Generate: openssl rand -hex 32

LARAVEL_API_KEY=LARAVEL_API_KEY=



# Security (Optional)# Security (Optional)

# Optional - untuk production dengan authentication# Optional - untuk production dengan authentication

# Generate: openssl rand -hex 32# Generate: openssl rand -hex 32

WA_API_KEY=WA_API_KEY=



# Puppeteer Configuration (untuk Linux)# Puppeteer Configuration (untuk Linux)

PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=truePUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromiumPUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium



# ========================================# ========================================

# CARA GENERATE SECURE KEYS:# CARA GENERATE SECURE KEYS:

# ========================================# ========================================

# Di server Linux, jalankan:# Di server Linux, jalankan:

# openssl rand -hex 32  # untuk API Key (32 chars)# openssl rand -hex 32  # untuk API Key (32 chars)

# openssl rand -hex 64  # untuk JWT Secret (64 chars)# openssl rand -hex 64  # untuk JWT Secret (64 chars)

# openssl rand -base64 32  # untuk Webhook Secret (32 chars)# openssl rand -base64 32  # untuk Webhook Secret (32 chars)

##

# Di Windows PowerShell:# Di Windows PowerShell:

# -o [System.Convert]::ToBase64String((1..32 | ForEach-Object {Get-Random -Minimum 0 -Maximum 256}))# -o [System.Convert]::ToBase64String((1..32 | ForEach-Object {Get-Random -Minimum 0 -Maximum 256}))

